"""Research orchestrator Python package (Phase 10+).

Thin in-repo package housing the orchestrator helpers that unit tests import
directly. The authoritative runtime copies also exist under
``~/.claude/skills/research-orchestrator/scripts/`` so the skill can invoke
them at runtime; the two copies are kept in lockstep by Plan 10-02.
"""
